# =============================================================================
# 依赖项 (Dependencies)
# node_modules 目录包含了所有通过 npm 安装的库，它体积巨大且可以通过 npm install 重新生成。
# 这是所有 Node.js 项目都必须忽略的目录。
# =============================================================================
/node_modules


# =============================================================================
# 构建产物和缓存 (Build Output & Cache)
# dist 目录是 Vite 打包后生成的最终插件文件，我们只提交源码，不提交构建结果。
# .vite 目录是 Vite 的开发缓存。
# =============================================================================
/dist
/.vite/


# =============================================================================
# 日志文件 (Log files)
# 由 npm 或其他工具在发生错误时生成的日志文件。
# =============================================================================
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# =============================================================================
# 环境变量 (Environment Variables)
# .env 文件通常包含敏感信息或本地特有的配置，不应被版本控制。
# =============================================================================
.env
.env.local
.env.*.local


# =============================================================================
# IDE 和编辑器配置 (IDE & Editor specific files)
# 忽略 VS Code, JetBrains 等编辑器的本地工作区配置文件。
# =============================================================================
.vscode/
.idea/


# =============================================================================
# 操作系统生成的文件 (Operating System files)
# 忽略 macOS 和 Windows 系统自动生成的元数据文件。
# =============================================================================
.DS_Store
Thumbs.db